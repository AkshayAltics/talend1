package routines;

import java.io.FileInputStream;
import java.io.IOException;
import jxl.Sheet;
import jxl.Workbook;
import jxl.read.biff.BiffException;
import java.util.HashMap;

public class ExcelRoutines {

   
    public static void helloExample(String message) {
        if (message == null) {
            message = "World"; //$NON-NLS-1$
        }
        System.out.println("Hello " + message + " !"); //$NON-NLS-1$ //$NON-NLS-2$
    }
    
    public static void readExcel() throws BiffException, IOException {
		String FilePath = "F:\\Talend workspacce\\EMPExcelSampleData.xls";
		FileInputStream fs = new FileInputStream(FilePath);
		Workbook wb = Workbook.getWorkbook(fs);

		// TO get the access to the sheet
		Sheet sh = wb.getSheet("Sheet1");
		
		// To get the number of rows present in sheet

	
		String shn[]=wb.getSheetNames();
	    HashMap<String, String> sheetdata = new HashMap<String, String>();	
	    String set=superSetOfColumns();
for(int s=0;s<wb.getSheetNames().length;s++)
{

		System.out.println("nothing "+shn[s]);

		sh=wb.getSheet(s);
		int totalNoOfRows = sh.getRows();
		// To get the number of columns present in sheet
		int totalNoOfCols = sh.getColumns();

		//System.out.println("total no of columns "+totalNoOfCols);
		//System.out.println("no of rows "+totalNoOfRows);
		for (int row = 0; row < totalNoOfRows; row++) {
			sheetdata.remove(row);
      // String colname[] ={"ID","FirstName","LastName","City","State"};
			for (int col = 0; col < totalNoOfCols; col++) {
				String test="";
				String data = sh.getCell(col, row).getContents();
				String val = sh.getCell(col,0).getContents();
				//String val = colname[col];
				if(val=="")
				{
					 data = sh.getCell(col, row).getContents();
					System.out.println("here is the null column is "+sh.getCell(col,0).getContents());
				
				sheetdata.put(val,"null");
				}
				else
				{
					//System.out.println("here is the not null data"+sh.getCell(col, row).getContents());
					sheetdata.put(sh.getCell(col,0).getContents(),data);
				}
			//	sheetdata.put("FirstName",sh.getCell(col, row).getContents());
			//	sheetdata.put("LastName",sh.getCell(col, row).getContents());
				//sheetdata.put("City",sh.getCell(col, row).getContents());
				//sheetdata.put("State",sh.getCell(col, row).getContents());
				
				
				//System.out.println("name of \t"+val);
				
				
				
		}
			//System.out.println(sheetdata);
			//System.out.println("\t");
		}
		
	    }


    }
    
    
    public void superSetOfColumns()
    {
    	
    }
    

}
